version: '3.8'
services:
  stock-alert:
    build: .
    volumes:
      - /NAS_SYS/docker/stock_alert/logs:/app/logs
      - /NAS_SYS/docker/stock_alert/.env:/app/.env
    environment:
      - NODE_ENV=production
    restart: unless-stopped